# Kurisko QR Pro Scanner

Slim VPS deployment — John Kurisko K1 quad rotation dashboard (Capital.com).

See `docs/KURISKO_QR_VPS.md` for architecture and TradingView webhook setup.

```bash
npm install
cp .env.example .env
npm run build && npm start
```
