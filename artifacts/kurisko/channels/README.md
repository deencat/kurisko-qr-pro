# Channel visual samples

Regenerate everything (HTML + PNG + summary) with:

```bash
npm run k1:channel-visual
```

Committed `*.png` files are small review samples. HTML/JSON stay local (gitignored).

See `docs/CHANNEL_VISUAL_CHECK.md`.
